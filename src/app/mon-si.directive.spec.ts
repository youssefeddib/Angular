import { MonSiDirective } from './mon-si.directive';

describe('MonSiDirective', () => {
  it('should create an instance', () => {
    const directive = new MonSiDirective();
    expect(directive).toBeTruthy();
  });
});
